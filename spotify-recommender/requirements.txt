# Serving dependencies (used by the deployed Streamlit app).
# For training (train.py) install requirements-train.txt instead.
# Loose lower-bounds so pip can pick prebuilt wheels for the host's Python.
streamlit>=1.35
spotipy>=2.23
pandas>=2.2
numpy>=1.26
pyarrow>=15
python-dotenv>=1.0
requests>=2.31
